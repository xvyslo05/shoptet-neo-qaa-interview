{
  "name": "qaa-interview-nameday",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend",
    "contracts",
    "e2e"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names backend,frontend \"npm run dev --workspace=@qaa/backend\" \"npm run dev --workspace=@qaa/frontend\"",
    "lint": "eslint .",
    "test": "npm test --workspace=@qaa/backend && npm test --workspace=@qaa/frontend && npm test --workspace=@qaa/contracts",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "concurrently": "^9.2.1",
    "eslint": "^9.34.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.41.0"
  },
  "engines": {
    "node": "22.x"
  }
}
